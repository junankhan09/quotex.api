[project]
name = "quotex-api"
version = "1.0.0"
description = "Trading Signals API for JUMMUU BOT"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["wrangler"]
build-backend = "wrangler.build"